# Locked dependencies (single source of truth)
#
# This project aims to be reproducible: `pip install -r requirements.txt` should
# always resolve to the same set of package versions.
#
# Optional heavyweight dependencies (e.g. diarization) MUST NOT be added here.
# Use `requirements.optional-diarization.txt` instead.

fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
celery==5.3.6
redis==5.0.1
flower==2.0.1
moviepy==1.0.3
faster-whisper==1.0.3
av==12.3.0
torch==2.4.0
openai==1.12.0
tenacity==8.2.3
numpy==1.26.3
pydantic==2.6.0
pydantic-settings==2.1.0
python-dotenv==1.0.1
psutil==5.9.7

# Test dependencies (kept in the same lock to keep CI simple and deterministic)
pytest==8.3.2
pytest-cov==5.0.0
pytest-timeout==2.3.1
httpx==0.27.0
